#! /bin/bash

Title=$1
Categories=$2
Tags=$3

DisqusIdentifier=$(./bin/data.py -di -f static.json)
PostName=$(./bin/newpost.py -t "$Title" -c "$Categories" -tg "$Tags" -cm -di "$DisqusIdentifier" -p)
vi $PostName
